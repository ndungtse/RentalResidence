server:
  port: 7070
spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  profiles:
    active: dev
  output:
    ansi:
      enabled: always
app:
  jwt:
    secret: rentalJwtSecretKey
    expiresIn: 86400000
  authorities:
    USER: USER
    ADMIN: ADMIN
